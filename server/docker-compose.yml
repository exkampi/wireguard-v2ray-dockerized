services:
  wg:
    image: linuxserver/wireguard:latest
    container_name: wg
    restart: unless-stopped
    working_dir: /config
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - SERVERURL=auto
      - SERVERPORT=51820
      - PEERS=1
      - PEERDNS=1.1.1.1,1.0.0.1
      - INTERNAL_SUBNET=10.13.13.0
      - PERSISTENTKEEPALIVE_PEERS=25
      - ALLOWEDIPS=10.13.13.4/32
    cap_add:
      - SYS_MODULE
      - NET_ADMIN
    expose:
      - 51820/udp
    volumes:
      - ../client/client.conf:/config/peer1/peer1.conf
      - ./wg/gen-keys.sh:/app/gen-keys.sh
      - /lib/modules:/lib/modules
    entrypoint: |
      sh -c "
        chmod +x /app/gen-keys.sh
        /app/gen-keys.sh
        exec /init
      "
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
    networks:
      v2:
        ipv4_address: 10.13.13.2

  v2ray:
    image: v2fly/v2fly-core:latest
    container_name: v2ray
    restart: unless-stopped
    expose:
      - 51820/udp
    ports:
      - 60123:1433/tcp
      - 60123:1433/udp
    volumes:
      - ./v2ray:/app/v2ray
      - ./init-scripts:/app/init-scripts
    entrypoint: |
      sh -c "
        chmod +x /app/init-scripts/init-v2ray-server.sh &&
        /app/init-scripts/init-v2ray-server.sh
        v2ray run -c /app/v2ray/v2config.json
      "
    networks:
      v2:
        ipv4_address: 10.13.13.3

networks:
  v2:
    name: v2
    external: true